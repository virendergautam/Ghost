<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{meta_title}}</title>
    <link rel="preload" as="style" href="{{asset "./built/main.css"}}">
    <link rel="stylesheet" href="{{asset "./built/main.css"}}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />

    {{ghost_head}}
    <style>
    :root {
        --headerBg: {{@custom.header_background}};
        --primaryBg: {{@custom.primary_background}};
        --primaryText: {{@custom.primary_text}};
        --secondaryBg: {{@custom.secondary_background}};
        --secondaryText: {{@custom.secondary_text}};
        --headingFont: '{{@custom.heading_font}}', sans-serif;
        --bodyFont: '{{@custom.body_font}}', sans-serif;
        --darkMode: {{#if @custom.dark_mode}}1{{else}}0{{/if}};
    }
</style>
    
</head>

<body>

    {{!-- {{> header}} --}}

    {{{body}}}

{{!-- <script>
document.addEventListener("DOMContentLoaded", function () {
    const tagCards = document.querySelectorAll(".tag-specific-card");
    const posts = document.querySelectorAll(".tag-specifc-post");
    const container = document.getElementById("postsContainer");

    tagCards.forEach(card => {
        card.addEventListener("click", () => {
            console.log(card.dataset.tags,"card.dataset.tag")
            const selectedTag = card.dataset.tags.toLowerCase();

            // Clear all posts
            posts.forEach(p => p.style.display = "none");

            // Show only posts that include this tag
            posts.forEach(p => {
            console.log(p.dataset.tags,"p.dataset.tags")

                const tags = p.dataset.tags.split(",");
                if(selectedTag=="all"){
                    p.style.display = "block"
                }
                else if (tags.includes(selectedTag)) {
                    p.style.display = "block";
                }
            });
        });
    });
});
</script> --}}

    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
        const swiper = new Swiper(".mySwiper", {
            slidesPerView: 2,
            spaceBetween: 40,
            loop: true,
            navigation: {
                nextEl: "#team-swiper-btn-next",
                prevEl: "#team-swiper-btn-prev",
            },
            breakpoints: {
                768: { slidesPerView: 5 },
                1024: { slidesPerView: 9 },
            }
        });
    </script>

    <script>
        const swiper2 = new Swiper(".mySwiper2", {
            slidesPerView: 1,
            spaceBetween: 20,
            loop: true,
            navigation: {
                nextEl: "#rec-swiper-btn-next",
                prevEl: "#rec-swiper-btn-prev",
            },
            breakpoints: {
                768: { slidesPerView: 2 },
                1024: { slidesPerView: 4 },
            }
        });
    </script>

    <script>
  const html = document.documentElement;
  const toggleBtn = document.getElementById("theme-toggle");
  const lightIcon = document.getElementById("theme-toggle-light-icon");
  const darkIcon = document.getElementById("theme-toggle-dark-icon");

  // Load previous mode
  if (localStorage.theme === 'dark' || 
     (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    html.classList.add('dark');
    darkIcon.classList.add('hidden');
    lightIcon.classList.remove('hidden');
  } else {
    html.classList.remove('dark');
    lightIcon.classList.add('hidden');
    darkIcon.classList.remove('hidden');
  }

  // Toggle
  toggleBtn.addEventListener("click", () => {
    const html = document.documentElement;

    const isDark = html.classList.contains("dark");

    if (isDark) {
        // Switch to LIGHT
        html.classList.remove("dark");
        html.classList.add("light");

        localStorage.theme = "light";

        lightIcon.classList.add("hidden");
        darkIcon.classList.remove("hidden");
    } else {
        // Switch to DARK
        html.classList.remove("light");
        html.classList.add("dark");

        localStorage.theme = "dark";

        darkIcon.classList.add("hidden");
        lightIcon.classList.remove("hidden");
    }
});
</script>


</body>

</html>